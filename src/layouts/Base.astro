---
import '../styles/global.css';
import '@fontsource-variable/inter';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonicalUrl?: string;
  schema?: object | object[];
  noindex?: boolean;
  ogType?: string;
  prevUrl?: string;
  nextUrl?: string;
  // Article-specific OG metadata
  articlePublished?: string;
  articleModified?: string;
  articleAuthor?: string;
}

const {
  title,
  description = 'Guías y tutoriales sobre cómo cambiar pilas y baterías de llaves de coche, auriculares, relojes y dispositivos electrónicos.',
  ogImage,
  canonicalUrl,
  schema,
  noindex = false,
  ogType = 'website',
  prevUrl,
  nextUrl,
  articlePublished,
  articleModified,
  articleAuthor = 'Cambiando de Pilas',
} = Astro.props;

const siteUrl = 'https://cambiandopilas.com';
const fullCanonical = canonicalUrl || new URL(Astro.url.pathname, siteUrl).href;
const resolvedOgImage = ogImage
  ? (ogImage.startsWith('http') ? ogImage : siteUrl + ogImage)
  : siteUrl + '/og-default.svg';
const schemas = schema ? (Array.isArray(schema) ? schema : [schema]) : [];
---

<!doctype html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={fullCanonical} />
  {noindex && <meta name="robots" content="noindex,nofollow" />}
  {prevUrl && <link rel="prev" href={siteUrl + prevUrl} />}
  {nextUrl && <link rel="next" href={siteUrl + nextUrl} />}

  <!-- Open Graph -->
  <meta property="og:type" content={ogType} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={fullCanonical} />
  <meta property="og:site_name" content="Cambiando de Pilas" />
  <meta property="og:locale" content="es_ES" />
  <meta property="og:image" content={resolvedOgImage} />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />

  <!-- Open Graph article metadata -->
  {articlePublished && <meta property="article:published_time" content={articlePublished} />}
  {articleModified  && <meta property="article:modified_time"  content={articleModified} />}
  {articleAuthor   && ogType === 'article' && <meta property="article:author" content={articleAuthor} />}

  <!-- OG image dimensions (1200×630 standard) -->
  <meta property="og:image:width"  content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:type"   content="image/svg+xml" />

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

  <!-- Fonts: preload critical woff2 then let fontsource handle the rest -->

  <!-- Schema.org -->
  {schemas.map((s) => (
    <script type="application/ld+json" set:html={JSON.stringify(s)} />
  ))}

  <!-- Google AdSense -->
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-1887931946230317" crossorigin="anonymous"></script>
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <a href="/" class="site-logo">
        <span>Cambiando de Pilas</span>
      </a>

      <button class="nav-toggle" aria-label="Abrir menú" id="navToggle">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M3 12h18M3 6h18M3 18h18" />
        </svg>
      </button>

      <nav class="main-nav" id="mainNav">
        <a href="/">Inicio</a>
        <a href="/categoria/blog">Llaves de coche</a>
        <a href="/categoria/audi">Audi</a>
        <a href="/categoria/renault">Renault</a>
        <a href="/categoria/volkswagen">Volkswagen</a>
        <a href="/categoria/mercedes-benz">Mercedes</a>
        <a href="/categoria/digital">Digital</a>
        <a href="/categoria/reloj">Reloj</a>
        <a href="/tipos-de-pilas">🔋 Guía</a>
        <a href="/blog">Blog</a>
      </nav>

      <button class="search-toggle" id="searchToggle" aria-label="Buscar">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20">
          <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
        </svg>
      </button>
    </div>
  </header>

  <!-- Search overlay -->
  <div class="search-overlay" id="searchOverlay" role="dialog" aria-modal="true" aria-label="Buscador">
    <div class="search-box">
      <button class="search-close" id="searchClose" aria-label="Cerrar búsqueda">✕</button>
      <input
        type="search"
        id="searchInput"
        class="search-input"
        placeholder="Buscar guías… (ej: CR2032, Audi A4, reloj Casio)"
        autocomplete="off"
      />
      <ul class="search-results" id="searchResults"></ul>
    </div>
  </div>

  <main>
    <slot />
  </main>

  <footer class="site-footer">
    <div class="container">
      <div class="footer-grid">
        <div class="footer-col">
          <h4>Categorías</h4>
          <a href="/categoria/blog">Llaves de coche</a>
          <a href="/categoria/audi">Audi</a>
          <a href="/categoria/renault">Renault</a>
          <a href="/categoria/volkswagen">Volkswagen</a>
          <a href="/categoria/mercedes-benz">Mercedes Benz</a>
          <a href="/categoria/digital">Digital</a>
          <a href="/categoria/reloj">Reloj</a>
        </div>
        <div class="footer-col">
          <h4>Guías por marca</h4>
          <a href="/marca/audi">Pilas llave Audi</a>
          <a href="/marca/renault">Pilas llave Renault</a>
          <a href="/marca/volkswagen">Pilas llave Volkswagen</a>
          <a href="/marca/mercedes">Pilas llave Mercedes</a>
          <a href="/tipos-de-pilas">Tipos de pilas</a>
          <a href="/blog">Todos los artículos</a>
        </div>
        <div class="footer-col">
          <h4>Sitio</h4>
          <a href="/sobre-nosotros">Sobre nosotros</a>
          <a href="/politica-de-privacidad">Política de privacidad</a>
          <a href="/politica-de-cookies">Política de cookies</a>
          <a href="/aviso-legal">Aviso legal</a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} cambiandopilas.com — Guías para cambiar pilas y baterías.</p>
      </div>
    </div>
  </footer>

  <!-- Scroll to top button -->
  <button id="scrollTop" class="scroll-top" aria-label="Volver arriba" title="Volver arriba">↑</button>

  <script>
    // Nav toggle
    document.getElementById('navToggle')?.addEventListener('click', () => {
      document.getElementById('mainNav')?.classList.toggle('active');
    });

    // Scroll to top
    const btn = document.getElementById('scrollTop');
    window.addEventListener('scroll', () => {
      if (btn) btn.style.opacity = window.scrollY > 400 ? '1' : '0';
    }, { passive: true });
    btn?.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));

    // Search overlay
    const searchToggle = document.getElementById('searchToggle');
    const searchOverlay = document.getElementById('searchOverlay');
    const searchInput = document.getElementById('searchInput') as HTMLInputElement;
    const searchResults = document.getElementById('searchResults');
    const searchClose = document.getElementById('searchClose');

    let posts: { title: string; slug: string; excerpt: string }[] = [];

    searchToggle?.addEventListener('click', async () => {
      searchOverlay?.classList.add('active');
      searchInput?.focus();
      if (posts.length === 0) {
        try {
          const r = await fetch('/search-index.json');
          posts = await r.json();
        } catch {}
      }
    });

    searchClose?.addEventListener('click', () => {
      searchOverlay?.classList.remove('active');
      if (searchInput) searchInput.value = '';
      if (searchResults) searchResults.innerHTML = '';
    });

    searchOverlay?.addEventListener('click', (e) => {
      if (e.target === searchOverlay) searchClose?.click();
    });

    searchInput?.addEventListener('input', () => {
      const q = searchInput.value.toLowerCase().trim();
      if (!searchResults) return;
      if (q.length < 2) { searchResults.innerHTML = ''; return; }
      const matches = posts
        .filter(p => p.title.toLowerCase().includes(q) || p.excerpt.toLowerCase().includes(q))
        .slice(0, 8);
      if (matches.length === 0) {
        searchResults.innerHTML = '<li class="search-noresult">Sin resultados para "' + searchInput.value + '"</li>';
      } else {
        searchResults.innerHTML = matches.map(p =>
          `<li><a href="/${p.slug}">${p.title}</a><span>${p.excerpt}</span></li>`
        ).join('');
      }
    });

    // Close on Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') searchClose?.click();
    });
  </script>
</body>
</html>
